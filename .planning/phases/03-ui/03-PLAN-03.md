---
phase: "03"
plan: "03"
type: execute
wave: 2
depends_on:
  - 03-PLAN-01
files_modified:
  - EasyTierGUI/Views/LogView.swift
autonomous: true
requirements:
  - UI-05
---

# Plan 03-03: 日志视图优化

<objective>
优化日志视图的颜色区分和可读性，添加级别图标，使 ERROR/WARN/INFO 更容易区分
</objective>

<tasks>

## Task 1: 添加日志级别图标

**Type:** execute
**Files:** EasyTierGUI/Views/LogView.swift

<read_first>
- EasyTierGUI/Views/LogView.swift (LogEntryRow 组件，第259-294行)
</read_first>

<action>
更新 LogEntryRow 组件，添加级别图标和优化颜色区分：

1. 添加级别图标属性：
```swift
private var levelIcon: String {
    switch entry.level.lowercased() {
    case "error", "err": return "exclamationmark.circle.fill"
    case "warn", "warning": return "exclamationmark.triangle.fill"
    case "debug", "trace": return "ant.fill"
    default: return "info.circle.fill"
    }
}
```

2. 更新 logBackgroundColor（保持现有逻辑）：
```swift
private var logBackgroundColor: Color {
    switch entry.level.lowercased() {
    case "error", "err":
        return Color.red.opacity(0.1)  // 从 0.08 提升到 0.1
    case "warn", "warning":
        return Color.orange.opacity(0.1)  // 从 0.08 提升到 0.1
    default:
        return showIndex % 2 == 0 ? Color(NSColor.controlBackgroundColor).opacity(0.2) : Color.clear
    }
}
```

3. 添加级别文字颜色：
```swift
private var levelColor: Color {
    switch entry.level.lowercased() {
    case "error", "err": return .red
    case "warn", "warning": return .orange
    case "debug", "trace": return .secondary
    default: return .primary
    }
}
```

4. 修改 body 更新 HStack 结构：
```swift
var body: some View {
    HStack(alignment: .top, spacing: 12) {
        // 级别图标
        Image(systemName: levelIcon)
            .font(.system(size: 12))
            .foregroundColor(levelColor)
            .frame(width: 16)
            .padding(.top, 4)
        
        // 时间戳
        Text(entry.timestamp.formatted(date: .omitted, time: .standard))
            .font(.system(.caption, design: .monospaced))
            .foregroundColor(.secondary)
            .frame(width: 80, alignment: .leading)
            .padding(.top, 4)
        
        // 消息内容
        Text(entry.message)
            .font(.system(.body, design: .monospaced))
            .foregroundColor(.primary.opacity(0.9))
            .textSelection(.enabled)
            .lineLimit(nil)
            .padding(.top, 2)
    }
    .padding(.vertical, 8)
    .padding(.horizontal, 12)
    .background(logBackgroundColor)
    .cornerRadius(8)
}
```
</action>

<verify>
- [ ] grep -n "levelIcon\|levelColor" EasyTierGUI/Views/LogView.swift
</verify>

<acceptance_criteria>
- `EasyTierGUI/Views/LogView.swift` 包含级别图标
- `./build.sh` 编译成功
</acceptance_criteria>

</tasks>

<tasks>

## Task 2: 优化日志级别文字显示

**Type:** execute
**Files:** EasyTierGUI/Views/LogView.swift

<read_first>
- EasyTierGUI/Views/LogView.swift (LogEntryRow 组件)
</read_first>

<action>
添加日志级别标签显示，使日志级别一目了然：

1. 在 LogEntryRow 中添加级别标签视图：
```swift
private var levelBadge: some View {
    let displayLevel = entry.level.uppercased()
    return Text(displayLevel)
        .font(.system(size: 9, weight: .bold, design: .monospaced))
        .foregroundColor(levelColor)
        .padding(.horizontal, 6)
        .padding(.vertical, 2)
        .background(levelColor.opacity(0.15))
        .cornerRadius(4)
}
```

2. 在 HStack 中级别图标和时间戳之间添加级别标签：
```swift
HStack(alignment: .top, spacing: 12) {
    Image(systemName: levelIcon)
        .font(.system(size: 12))
        .foregroundColor(levelColor)
        .frame(width: 16)
        .padding(.top, 4)
    
    levelBadge  // 新增：级别标签
    
    Text(entry.timestamp.formatted(date: .omitted, time: .standard))
        ...
}
```

3. 调整时间戳 frame 宽度从 80 改为 60（为级别标签留空间）：
```swift
.frame(width: 60, alignment: .leading)
```
</action>

<verify>
- [ ] grep -n "levelBadge" EasyTierGUI/Views/LogView.swift
</verify>

<acceptance_criteria>
- `EasyTierGUI/Views/LogView.swift` 包含级别标签
- `./build.sh` 编译成功
</acceptance_criteria>

</tasks>

</tasks>

<verification>
## Verification Steps
1. 运行 `./build.sh` 确保编译成功
2. 手动检查日志颜色显示：
   - ERROR 级别：红色图标 + 红色级别标签 + 浅红背景
   - WARN 级别：橙色图标 + 橙色级别标签 + 浅橙背景
   - INFO/DEBUG：默认颜色
</verification>

<success_criteria>
## Success Criteria
- [ ] 日志 ERROR 级别显示红色图标和标签
- [ ] 日志 WARN 级别显示橙色图标和标签
- [ ] 日志 INFO/DEBUG 级别显示默认图标和颜色
- [ ] 整体颜色对比度提升，可读性增强
- [ ] 构建成功无错误
</success_criteria>

<must_haves>
## Must Haves (Goal-Backward Verification)
- [ ] UI-05: 日志视图颜色区分，易读性好（级别图标 + 级别标签）
</must_haves>
